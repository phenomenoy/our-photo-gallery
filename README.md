# Photo Gallery

一个移动端优先的静态照片展厅，适合部署到 GitHub Pages。

## 本地预览

```powershell
python -m http.server 8788 --bind 127.0.0.1
```

打开 `http://127.0.0.1:8788/`。

## GitHub Pages

仓库推送到 GitHub 后，`.github/workflows/pages.yml` 会自动发布静态站点。
